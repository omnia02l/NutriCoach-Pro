<form method="post" action="{{ path('app_convention_delete', {'id': convention.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ convention.id) }}">
    <button class="btn">Delete</button>
</form>
